1. CRITICAL — `src/components/AffiliateInlineCTA.tsx:20` — Both the spec and plan miss the user-visible hardcoded price `🏨 Hotels from $79`. This component renders on article pages alongside the affiliate sidebar, so the claimed cleanup still leaves an invented hotel price on a live monetization surface, and the guard cannot detect it because the file is outside its scope. Concrete fix: remove the price from this CTA and add `src/components/AffiliateInlineCTA.tsx` to the AST guard scope.

2. CRITICAL — `src/app/[locale]/hot-deals/page.tsx:106` — Both documents leave `Late-night hotel deals` unchanged. This is fabricated time-bound urgency equivalent to the explicitly prohibited `tonight`, but `/tonight/i` and `/last[-\s]?minute/i` do not match `Late-night`. Concrete fix: rewrite it to a non-urgent category label such as `Hotel deals`, and add `/late[-\s]?night/i` to the guard.

3. CRITICAL — `messages/en/common.json:343` — The plan knowingly leaves `Weekly flight + hotel bundles with price-drop alerts.` on the hot-deals page. The five translations at `messages/{de,es,fr,it,pt}/common.json:343` make the same promise. `NewsletterForm` only POSTs `{email, source}` to `/api/newsletter` (`src/components/NewsletterForm.tsx:13-17`), and the repository search found no price-drop notification worker or delivery path. Calling this “report-only” at plan lines 302-304 leaves a fabricated product feature live. Arm C also misses every version because it only prohibits price shapes and `live/feed/flux`. Concrete fix: remove or truthfully rewrite all six `dealAlertsDesc` values now and guard the approved copy or the translated false-claim phrases.

4. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:90-106` — The urgency arm runs only on F2, so existing fabricated claims in `affiliates.ts` are outside that arm: `Tonight's Deals` (`src/config/affiliates.ts:66`), `Save up to 40%` (`:69`), `Up to 75% off` (`:96`), `Save 35%` (`:106`), and `Weekly discounts` (`:124`). The plan removes them once but provides no recurrence protection. Concrete fix: apply the urgency/discount arm to F3 and add patterns for `save ... N%`, `weekly discounts`, and equivalent fabricated offer language.

5. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:98-106` — The regex set misses current fabricated offer text:
   - `CruiseDirect · 5-night all-inclusive from Port Everglades` at `src/app/[locale]/hot-deals/page.tsx:73`; as the plan itself notes, `5-night` does not match `/\/night/`.
   - `Includes meals, entertainment & port stops at Nassau + Cozumel` at `hot-deals/page.tsx:75`.
   - `CruiseDirect · 5-night all-inclusive` at `src/config/affiliates.ts:94`.
   - `Bahamas Cruise — 3 Nights` at `src/config/affiliates.ts:130`.
   These are removed by the planned edits but can return without failing the guard. Concrete fix: add targeted prohibited patterns for fabricated duration/itinerary/package claims, including `\b\d+[-\s]nights?\b`, or assert an explicit allowlist/schema for the remaining category copy.

6. MAJOR — `src/config/affiliates.ts:168,192` — Two discount claims rendered by `AffiliateSidebar` survive entirely: `Save 10%+ with Member Prices` and `Cruises Up to 75% Off`. Arm A does not prohibit percentages; Arm B applies only to `hot-deals/page.tsx`; Arm D merely checks banner URLs. The plan therefore claims to remove fabricated discount assertions while leaving discount assertions on the article monetization surface. Concrete fix: establish that these are current, partner-supplied creatives with evidence and encode that exception explicitly, or remove/rewrite them and cover `CJ_BANNERS` in the claim guard.

7. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:91-95` — The AST walk is not recurrence-safe. It ignores `NumericLiteral` and does not reconstruct constant expressions. User-visible forms such as `<span>${349}</span>`, `<span>{349}</span>`, `"$" + 349`, or `` `$${349}` `` evade the listed node/token checks because neither `"$"` nor the numeric token independently matches `/\$\s?\d/`. Concrete fix: inspect/evaluate constant JSX expressions and binary/template concatenations, or render/import the scoped data and inspect the resulting user-visible strings in addition to the source-token guard.

8. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:124-126` — Arm D is internally contradictory. `AffiliateSidebar` renders only `DEALS.slice(0, 3)` (`src/components/AffiliateSidebar.tsx:9`), so asserting “each deal’s `cta` text” for all eight deals is unsatisfiable. If “each deal” means only those three, five deal CTAs receive no render coverage. Concrete fix: explicitly assert the three rendered sidebar deals, then separately render `DesignA`, `PackageDealsCarousel`, and `/hot-deals`, covering every deal ID, CTA, and associated `href`.

9. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:124-127` — The `≥3 anchors` assertion is theater. `AffiliateSidebar` independently renders four `CJ_BANNERS` anchors (`src/components/AffiliateSidebar.tsx:30-55`), so all three deal anchors can disappear and the anchor-count assertion still passes. CTA text could also survive outside its anchor without proving monetization works. Concrete fix: query each expected CTA as a link and assert its exact `href`; do not use an aggregate anchor count.

10. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:119-126` — Arm D does not exercise the other live DEALS consumers: `DesignA` (`src/components/DesignA.tsx:56-75`), both DEALS loops on `/hot-deals` (`src/app/[locale]/hot-deals/page.tsx:84-98,138-154`), or `PackageDealsCarousel` (`src/components/PackageDealsCarousel.tsx:108-130`). Those surfaces can lose their CTA/link rendering while Arm D remains green. Concrete fix: add render or Playwright assertions for every consumer, pairing visible CTA text with its sponsored CJ `href`.

11. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:121-123` — The direct `CJ_LINKS` census is incomplete. It omits `hotelsMemberPrices`, `cruisesHoneymoon`, `cruisesFamily`, `cruisesBahamas`, `cruisesCarnival`, `cruisesDisney`, `cruisesCelebrity`, and `carsCompare` from `src/config/affiliates.ts:8,17-23,27`. Several are consumed elsewhere, including `carsCompare` and `cruisesLastMinute` in `ArticleAffiliateCTA` and `AffiliateRecommendations`. Concrete fix: iterate all `CJ_LINKS` functions with required arguments where necessary, explicitly exempt only `airAdvisor` because it is documented as a pending non-CJ URL, and test each live consumer’s link.

12. CRITICAL — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:155,164-166` — `Member prices available` is attached to both Hotels.com deals, but `getAffiliateUrl()` routes them through `CJ_LINKS.hotels()` (`src/config/affiliates.ts:143`), not `CJ_LINKS.hotelsMemberPrices()`. The named member-prices function has zero consumers. The replacement therefore advertises a benefit that the actual landing link does not establish. Concrete fix: use neutral copy, or deliberately route those deals through the verified member-prices campaign and test the exact destination.

13. CRITICAL — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:158,162,164-166` — `No booking fees` is not backed “elsewhere in the repo.” The only source occurrence is the very target string being scrubbed at `src/app/[locale]/hot-deals/page.tsx:119`; the plan cites that claim as provenance for itself. Concrete fix: remove it unless verified against current CruiseDirect program terms or creative documentation.

14. CRITICAL — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:161` — `Weekly stays available` does not exist anywhere else in the source tree. It is newly invented replacement copy. Concrete fix: replace it with a neutral category description already true from the data, such as `Vrbo · Manhattan & Brooklyn stays`, and do not fabricate a badge merely to preserve layout.

15. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:156,160,164-166` — `Free cancellation` is only repeated in the same unverified target surfaces: Vrbo at `src/config/affiliates.ts:78` and `hot-deals/page.tsx:113`, and EconomyBookings at `affiliates.ts:113,115` and `hot-deals/page.tsx:125`. Repetition of existing marketing copy is not partner evidence, and a generic affiliate landing URL does not guarantee cancellation terms for every result. Concrete fix: use neutral copy unless current partner terms or approved CJ creative substantiates the claim.

16. MAJOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:157,164-166` — `Compare 500+ suppliers` does have another repository occurrence at `src/components/ArticleAffiliateCTA.tsx:39`, but that is another hardcoded TPI assertion, not source-of-truth evidence. Duplicating it into the green `savings` badge increases exposure without validation. Concrete fix: verify it from current EconomyBookings/CJ materials or replace it with non-quantified `Compare car-rental suppliers`.

17. VERIFIED-OK — A/count and current token census: the exact scoped price-literal count is 41: 24 in `destinations/page.tsx`, 5 in `hot-deals/page.tsx`, and 12 in `affiliates.ts`. `$349` at `hot-deals/page.tsx:74` is `JsxText` and is caught; `5-night` is not matched. The four EPC comments at `affiliates.ts:7,8,13,17` are excluded by the AST walk. The scope is not repo-wide and therefore avoids the known unsatisfiable ban.

18. VERIFIED-OK — D: repository search found exactly five direct `AffiliateDeal.price` render references: `AffiliateSidebar.tsx:21`, `DesignA.tsx:70`, `PackageDealsCarousel.tsx:124`, and `hot-deals/page.tsx:96,148`. No indirect spread, serialization, snapshot, `Object.values`, or additional `AffiliateDeal.price` consumer was found. `scoreDeal()` at `PackageDealsCarousel.tsx:17-29` does not use price.

19. VERIFIED-OK — C/edit instructions: if followed literally, the planned production edits do not delete any current DEALS/CJ CTA, ID, override URL, or direct `CJ_LINKS` href. The defect is the inadequate regression test, not a link explicitly removed by the edit table.

20. MINOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:284` — The lint gate requires only `0 errors`, so warnings can regress above the verified baseline of 30 and still pass. Concrete fix: require `0 errors` and no increase above 30 warnings, or run ESLint with a baseline-enforcing wrapper.

21. MINOR — `docs/superpowers/plans/2026-07-13-kill-hardcoded-prices.md:60-63,320` — The process evidence is stale. PID `885336` no longer exists; port 3001 is currently PID `911990`, whose cwd is this exact worktree. Blindly treating every listener as foreign can kill the correct server. Concrete fix: resolve `/proc/$pid/cwd`, kill only a listener owned by another worktree, and verify the replacement server’s cwd before E2E.

22. VERIFIED-OK — F: `vitest.config.ts:10` includes `src/**/*.test.{ts,tsx}`, so `src/test/no-fabricated-claims.test.ts` will be discovered. Fresh baselines are 13 unit files/80 tests, ESLint 0 errors/30 warnings, and Playwright lists exactly 41 tests.

VERDICT: NO.
