// Stub — next-intl middleware routes /account/preferences/ to [locale=en]/account/preferences/ before this is reached.
// Canonical page: src/app/[locale]/account/preferences/page.tsx
export const dynamic = "force-dynamic";
export default function PreferencesStub() { return null; }
