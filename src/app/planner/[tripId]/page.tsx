// Stub — next-intl middleware routes /planner/[tripId]/ to [locale=en]/planner/[tripId]/ before this is reached.
// Canonical page: src/app/[locale]/planner/[tripId]/page.tsx
export const dynamic = "force-dynamic";
export default function TripStub() { return null; }
