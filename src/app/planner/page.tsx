// Stub — next-intl middleware routes /planner/ to [locale=en]/planner/ before this is reached.
// Canonical page: src/app/[locale]/planner/page.tsx
export const dynamic = "force-dynamic";
export default function PlannerStub() { return null; }
