// Stub — next-intl middleware routes /guides/ to [locale=en]/guides/ before this is reached.
// Canonical page: src/app/[locale]/guides/page.tsx
export const dynamic = "force-dynamic";
export default function GuidesStub() { return null; }
