// Stub — next-intl middleware routes /terms/ to [locale=en]/terms/ before this is reached.
// Canonical page: src/app/[locale]/terms/page.tsx
export const dynamic = "force-dynamic";
export default function TermsStub() { return null; }
