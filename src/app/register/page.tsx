// Stub — next-intl middleware routes /register/ to [locale=en]/register/ before this is reached.
// Canonical page: src/app/[locale]/register/page.tsx
export const dynamic = "force-dynamic";
export default function RegisterStub() { return null; }
